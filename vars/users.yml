---
users:
    - name: test_q
      ssh_key: "{{ test_ssh_key }}"
      group: qualcomm
    - name: test_h
      ssh_key: "{{ test_ssh_key }}"
      group: huawei
    - name: test_c
      ssh_key: "{{ test_ssh_key }}"
      group: cavium
